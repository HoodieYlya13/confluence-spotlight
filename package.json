{
  "name": "confluence-spotlight",
  "private": true,
  "version": "0.1.1",
  "description": "Tauri-based desktop Spotlight command bar for the RBAC-enforced Confluence RAG MCP server.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:beta": "tauri build --config src-tauri/tauri.beta.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
